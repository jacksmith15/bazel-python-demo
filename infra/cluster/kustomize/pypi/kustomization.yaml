apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- deployment.yaml
- ingress.yaml
# - persistent-volume.yaml
- service.yaml

configMapGenerator:
  - name: pypi-config
    namespace: pypi
    files:
      - .htpasswd=config/.htpasswd
